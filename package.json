{
  "name": "micro-dnd-monorepo",
  "private": true,
  "description": "The math-driven drag-and-drop engine",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "tsup": "^8.0.2",
    "vitest": "^1.5.0",
    "prettier": "^3.2.5"
  }
}
